# 剩余步骤核对与执行计划

更新时间：2026-07-10
工作区：E:\codex\GIS-SolutionWorkPlatform
依据文件：
- E:\workbuddy\GIS-AI-Platform-Prompt.md
- C:\Users\焦志龙\.codex\attachments\4b23bc19-8e69-4570-83ca-3654a28c685b\pasted-text.txt
- E:\codex\GIS-SolutionWorkPlatform\TODO.md

## 当前已完成

### Step 7：Skill 管理
状态：已完成核心 CRUD + 测试运行。

已落地：
- `skills` 表、实体、Mapper、Service、Controller
- `/api/skills` CRUD
- `/api/skills/{id}/test` 测试运行
- Prompt 模板变量替换
- 可选 IMA Mock、LLM 调用
- 写入 `system_logs`，`log_type=SKILL`
- 前端 `/skills` 页面

差异/待增强：
- 原计划里的独立 `SkillExecutor.java` 未单独拆出，目前执行逻辑在 `SkillServiceImpl` 内。
- 原计划要求预置 3 个 GIS Skill，目前需要再补充确认/完善种子数据。
- 前端已能测试运行，但不是原计划中拆成 `SkillEdit.tsx` / `SkillTest.tsx` 的目录结构。

### Step 8：流程编排
状态：已完成核心 DAG 流程编排与执行。

已落地：
- `flows`、`flow_nodes`、`flow_edges`、`flow_executions`
- Flow CRUD
- DAG 节点、边保存
- 拓扑排序执行
- 执行时调用 Skill 测试运行
- 执行结果写入 `flow_executions`
- 写入 `system_logs`，`log_type=FLOW`
- 前端 `/flows` 页面

差异/待增强：
- 原计划里的独立 `FlowEngineImpl.java` 未单独拆出，目前执行逻辑在 `FlowServiceImpl` 内。
- 前端当前是 JSON 方式编辑节点/边，不是拖拽式可视化 DAG 编辑器。
- 预置流程需要进一步整理成正式 seed。

### Step 8.5：用户登录 + GIS 3D 地球登录页 + 权限拦截
状态：已完成。

已落地：
- users 表字段补齐：`real_name`、`status`、`last_login_at`、`updated_at`
- 默认管理员账号：`admin / admin123`
- 后端认证接口：
  - `POST /api/auth/login`
  - `POST /api/auth/register`
  - `GET /api/auth/me`
  - `POST /api/auth/change-password`
- JWT 生成、解析、过期控制
- BCrypt 密码加密与校验
- `/api/**` 权限拦截：登录、注册、hello、Swagger 放行，其它接口需要 Token
- 前端 `/login` 登录页
- `frontend/src/components/GisGlobe/index.tsx` 3D GIS 地球
- auth store/localStorage 登录态保存
- axios 自动携带 `Authorization: Bearer <token>`
- 401 自动清理登录态并回到 `/login`
- AuthGuard 路由守卫
- 顶部用户信息和退出登录

验证记录：
- 无 Token 访问 `/api/projects` 返回 401
- 登录 `admin/admin123` 返回 `code=0` 和 JWT token
- 带 Token 访问 `/api/projects` 返回 `code=0`
- 通过 Vite 代理访问 `/api/auth/login`、`/api/projects` 验证通过
- 浏览器截图确认 `/login` 显示 GIS 3D 地球登录页
- 浏览器截图确认未登录访问 `/projects` 会跳转 `/login`

### Step 9：流程集成到项目
状态：已完成项目维度流程执行 + PPT 大纲记录闭环。

已落地：
- 项目详情中选择 Flow 执行
- 自动带入项目上下文
- `/api/projects/{projectId}/flows/{flowId}/run`
- `/api/projects/{projectId}/flow-executions`
- `/api/projects/{projectId}/ppt/outline/generate`
- `/api/projects/{projectId}/ppt`
- 使用 `flow_executions.project_id` 关联项目
- 使用 `ppt_records` 保存 PPT 大纲记录

差异/待增强：
- 原计划的 `proposal_records` 表和方案记录 Tab 未单独实现。
- 当前方案内容主要沉淀在 Flow 输出和 PPT 大纲记录里，还不是正式“方案记录库”。

### Step 11 中的日志页面部分
状态：已完成。

已落地：
- `/api/system/logs` 分页查询
- logType、level、module、keyword 筛选
- 统计卡片：总数、FLOW、错误数、平均耗时
- 前端 `/logs` 页面
- 侧边栏“系统日志”入口

## 当前未完成/需要补做

### 1. TODO-11 / 模板管理
状态：未完成。

已有基础：
- `templates` 表已在 init.sql 中存在
- `Template.java`、`TemplateMapper.java` 已存在

缺少内容：
- `TemplateController`
- `TemplateService` / `TemplateServiceImpl`
- DTO/VO
- CRUD 接口
- 前端 `/templates` 页面，目前仍是 Coming Soon
- 模板变量解析/高亮
- 预置 GIS 行业模板，例如智慧城市方案模板、投标模板

建议接口：
- `POST /api/templates`
- `GET /api/templates?type=&category=`
- `GET /api/templates/{id}`
- `PUT /api/templates/{id}`
- `DELETE /api/templates/{id}`

### 2. TODO-12 / PPT 内容编辑器
状态：未完成，需要单独做。

当前已有：
- `ppt_records` 表
- `PptRecord.java`、`PptRecordMapper.java`
- 项目详情中可以基于最近成功 Flow 生成 PPT 大纲记录
- 前端项目详情能看到 PPT 大纲记录

当前缺少：
- 真实 PPT Slide JSON 内容生成
- `PPT 内容生成` Skill
- `PPT 生成流程`：PPT 大纲 Skill -> PPT 内容 Skill
- 独立 PPT 接口：
  - `POST /api/ppt/generate`
  - `GET /api/ppt/records?projectId=`
  - `GET /api/ppt/records/{id}`
  - `PUT /api/ppt/records/{id}`
- 前端 PPT 编辑器：
  - 左侧缩略图列表
  - 主区标题/正文/备注编辑
  - 保存后刷新仍在
  - 从项目/方案生成 PPT 内容
- 当前阶段不要求导出 `.pptx`，但需要结构化编辑和保存。

结论：PPT 生成还需要单独做。现在完成的是“PPT 大纲记录”，不是完整的“PPT 内容生成 + 编辑器”。

### 3. TODO-13 / README 本地运行说明
状态：未完成。

需要补充：
- 项目简介
- 环境要求：JDK 11、Maven、Node、PostgreSQL/PostGIS
- 数据库配置：localhost:5432、postgres/postgis
- 初始化 SQL 说明
- 后端启动方式
- 前端启动方式
- 默认登录账号：`admin/admin123`
- 功能路径说明
- 常见问题：JDK source、端口占用、GitHub 推送网络问题、Vite 端口变化

## 建议后续执行顺序

1. TODO-11：模板管理 CRUD
2. TODO-12：PPT 内容生成 + PPT 编辑器
3. TODO-13：README 本地运行说明
4. 回补增强项：SkillExecutor/FlowEngine 独立拆分、预置 Skill/Flow seed、proposal_records 方案记录库

## PPT 是否需要单独做

需要。

原因：
- 当前项目里的 PPT 能力只到 `ppt_records.outline_json/content_json` 的大纲记录。
- 还没有真正的 Slide JSON 生成接口。
- 还没有 PPT 编辑器。
- 还没有从方案内容到 PPT 内容的完整流程。
- 还不能逐页编辑并保存标题、正文、备注。

因此 TODO-12 仍然是独立且必要的一步。
